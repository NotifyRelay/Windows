<Page
    x:Class="NotifyRelay.Views.Settings.ScrcpyAdbSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:NotifyRelay.Helpers"
    xmlns:items="using:NotifyRelay.Data.Items"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:NotifyRelay.ViewModels.Settings"
    xmlns:wuc="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:GeneralViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <ScrollViewer Padding="0,0,40,0">
        <StackPanel Spacing="4">
            <!--  Breadcrumb Navigation  -->
            <BreadcrumbBar x:Name="BreadcrumbBar" Margin="0,0,0,8">
                <BreadcrumbBar.ItemTemplate>
                    <DataTemplate x:DataType="items:BreadcrumbBarItemModel">
                        <BreadcrumbBarItem AutomationProperties.Name="{Binding Name}" Content="{Binding}">
                            <BreadcrumbBarItem.ContentTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        FontSize="28"
                                        FontWeight="SemiBold"
                                        Text="{Binding Name}" />
                                </DataTemplate>
                            </BreadcrumbBarItem.ContentTemplate>
                        </BreadcrumbBarItem>
                    </DataTemplate>
                </BreadcrumbBar.ItemTemplate>
            </BreadcrumbBar>

            <!--  Scrcpy location  -->
            <wuc:SettingsCard Description="{x:Bind ViewModel.ScrcpyPath, Mode=OneWay}" Header="{helpers:ResourceString Name=ScreenMirror}">
                <wuc:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xEC15;" />
                </wuc:SettingsCard.HeaderIcon>
                <Button Click="SelectScrcpyLocation_Click" Content="{helpers:ResourceString Name=SelectLocation}" />
            </wuc:SettingsCard>

            <!--  Adb Settings  -->
            <wuc:SettingsCard Description="{x:Bind ViewModel.AdbPath, Mode=OneWay}" Header="{helpers:ResourceString Name=AdbSettings}">
                <wuc:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xEC7A;" />
                </wuc:SettingsCard.HeaderIcon>
                <Button Click="SelectAdbLocation_Click" Content="{helpers:ResourceString Name=SelectLocation}" />
            </wuc:SettingsCard>

            <!--  Receive media messages  -->
            <wuc:SettingsCard Header="{helpers:ResourceString Name=ReceiveMediaMessages}">
                <wuc:SettingsCard.HeaderIcon>
                    <SymbolIcon Symbol="Play" />
                </wuc:SettingsCard.HeaderIcon>
                <ComboBox
                    MinWidth="180"
                    ItemsSource="{x:Bind ViewModel.MediaMessageReceiveModes.Values}"
                    SelectedItem="{x:Bind ViewModel.SelectedMediaMessageReceiveMode, Mode=TwoWay}" />
            </wuc:SettingsCard>
        </StackPanel>
    </ScrollViewer>
</Page>
