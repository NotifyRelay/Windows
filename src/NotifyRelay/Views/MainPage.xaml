<Page
    x:Class="NotifyRelay.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:NotifyRelay.Converters"
    xmlns:wctconverters="using:CommunityToolkit.WinUI.Converters"
    xmlns:helpers="using:NotifyRelay.Helpers"
    xmlns:local="using:NotifyRelay.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:NotifyRelay.Data.Models"
    xmlns:userControls="using:NotifyRelay.UserControls"
    xmlns:viewmodels="using:NotifyRelay.ViewModels"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    Background="{ThemeResource App.PageBackgroundBrush}"
    x:Name="PageRoot">

    <Page.Resources>
        <wctconverters:BoolToObjectConverter x:Key="BoolToConnectionButtonTextConverter"
            FalseValue="{helpers:ResourceString Name=Disconnected/Text}"
            TrueValue="{helpers:ResourceString Name=Connected/Text}" />
        
        <wctconverters:BoolToObjectConverter x:Key="BoolToPinTextConverter"
            FalseValue="{helpers:ResourceString Name=Pin}"
            TrueValue="{helpers:ResourceString Name=Unpin}" />
        
        <wctconverters:EmptyObjectToObjectConverter x:Key="EmptyObjectToOpacityConverter" EmptyValue="1.0" NotEmptyValue="0.0" />
        <wctconverters:BoolToObjectConverter x:Key="BoolToPinGlyphConverter" FalseValue="&#xE718;" TrueValue="&#xE77A;" />
        <!-- 不再使用，已移至 NotificationsListControl.xaml -->
        <wctconverters:BoolNegationConverter x:Key="BoolNegationConverter" />

        <converters:DateTimeConverter x:Key="DateTimeConverter" />
        <converters:RingerModeToIconConverter x:Key="RingerModeToIconConverter" />
        <converters:BatteryStatusToIconConverter x:Key="BatteryStatusToIconConverter" />
        <converters:BatteryStatusToColorConverter x:Key="BatteryStatusToColorConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" Inverse="True" />
        <converters:EmptyObjectToVisibilityConverter x:Key="EmptyObjectToVisibilityConverter" />
        <converters:EmptyObjectToVisibilityConverter x:Key="InverseEmptyObjectToVisibilityConverter" Inverse="True" />
        <converters:StringToImageSourceConverter x:Key="StringToImageSourceConverter" />
        <converters:AdbIconToTypeConverter x:Key="AdbIconToTypeConverter" />

        <!-- Phone Frame Overlay Animations -->
        <Storyboard x:Key="FadeInAnimation" x:Name="FadeInStoryboard">
            <DoubleAnimation 
                Storyboard.TargetName="PhoneFrameOverlay" 
                Storyboard.TargetProperty="Opacity" 
                From="0" 
                To="1" 
                Duration="0:0:0.200" />
        </Storyboard>
        
        <Storyboard x:Key="FadeOutAnimation" x:Name="FadeOutStoryboard">
            <DoubleAnimation 
                Storyboard.TargetName="PhoneFrameOverlay" 
                Storyboard.TargetProperty="Opacity" 
                From="1" 
                To="0" 
                Duration="0:0:0.200" />
        </Storyboard>
    </Page.Resources>

    <Grid
        AllowDrop="True"
        DragOver="Grid_DragOver"
        Drop="Page_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <win:Grid Grid.Row="0">
            <userControls:TitleBar />
        </win:Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!--  Left Panel: Device Info and Notifications  -->
            <Grid Grid.Column="0" Padding="0,0,5,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  Connected Device View  -->
                <Grid
                    Grid.Row="0"
                    Margin="18,0,0,15"
                    Visibility="{x:Bind ViewModel.Device, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid
                        x:Name="PhoneFrameGrid"
                        Grid.Column="0"
                        Width="53"
                        Height="100"
                        VerticalAlignment="Center"
                        CornerRadius="7"
                        PointerEntered="PhoneFrame_PointerEntered"
                        PointerExited="PhoneFrame_PointerExited"
                        PointerWheelChanged="PhoneFrame_PointerWheelChanged">
                        <!--  Phone frame border  -->
                        <Rectangle
                            Opacity="0.5"
                            RadiusX="4"
                            RadiusY="4"
                            Stroke="Black"
                            StrokeThickness="2">
                            <Rectangle.Fill>
                                <SolidColorBrush Opacity="0.1" Color="{ThemeResource SystemBaseLowColor}" />
                            </Rectangle.Fill>
                        </Rectangle>
                        <!--  Inner screen border  -->
                        <Rectangle
                            Margin="2"
                            RadiusX="4"
                            RadiusY="4"
                            Stroke="Black"
                            StrokeThickness="1" />
                        <!--  Wallpaper  -->
                        <Border Margin="3" CornerRadius="4">
                            <Border.Background>
                                <LinearGradientBrush Opacity="{x:Bind ViewModel.Device.Wallpaper, Converter={StaticResource EmptyObjectToOpacityConverter}, Mode=OneWay}" StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Offset="0" Color="#FF0070FF" />
                                    <GradientStop Offset="1" Color="#FF2E90FF" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Image
                                x:Name="WallpaperImage"
                                Source="{x:Bind ViewModel.Device.Wallpaper, Mode=OneWay}"
                                Stretch="UniformToFill"
                                Visibility="{x:Bind ViewModel.Device.Wallpaper, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
                        </Border>

                        <!--  Overlay that appears on hover  -->
                        <Grid
                            x:Name="PhoneFrameOverlay"
                            Background="#80000000"
                            Opacity="0"
                            Tapped="PhoneFrameOverlay_Tapped"
                            Visibility="Collapsed">
                            <FontIcon
                                Foreground="White"
                                Glyph="&#xEC15;"
                                Visibility="{x:Bind ViewModel.LoadingScrcpy, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <ToolTipService.ToolTip>
                                <ToolTip Content="Screen Mirror" />
                            </ToolTipService.ToolTip>
                        </Grid>

                        <!--  Loading indicator  -->
                        <Grid Visibility="{x:Bind ViewModel.LoadingScrcpy, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ProgressRing
                                Width="24"
                                Height="24"
                                Foreground="White"
                                IsActive="{x:Bind ViewModel.LoadingScrcpy, Mode=OneWay}" />
                        </Grid>
                    </Grid>

                    <!--  Device Info  -->
                    <StackPanel
                        Grid.Column="1"
                        Margin="15,0"
                        VerticalAlignment="Bottom"
                        Spacing="5">

                        <TextBlock
                            FontSize="14"
                            FontWeight="SemiBold"
                            Text="{x:Bind ViewModel.Device.Name, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.Device.Name, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />

                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="5"
                            Visibility="{x:Bind ViewModel.Device.ConnectionStatus, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">

                            <!--  Audio Mode Button  -->
                            <Button
                                Grid.Column="2"
                                Padding="5"
                                Background="Transparent"
                                BorderThickness="0">
                                <FontIcon Glyph="{x:Bind ViewModel.AudioStatusIcon, Mode=OneWay}" />
                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind ViewModel.AudioStatusText, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                                <Button.Flyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem
                                            Command="{x:Bind ViewModel.SetRingerModeCommand}"
                                            CommandParameter="2"
                                            Text="仅音频播放中">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xE995;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem
                                            Command="{x:Bind ViewModel.SetRingerModeCommand}"
                                            CommandParameter="0"
                                            Text="停止仅音频">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xE74F;" />
                                            </MenuFlyoutItem.Icon>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </Button.Flyout>
                            </Button>

                            <!--  ftp Button  -->
                            <Button
                                Padding="5"
                                Background="Transparent"
                                BorderThickness="0"
                                Command="{x:Bind ViewModel.StartftpConnectionCommand}"
                                ToolTipService.ToolTip="ftp 连接">
                                <FontIcon Glyph="&#xE8A5;" />
                            </Button>

                            <!--  Battery status  -->
                            <StackPanel
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="1"
                                Visibility="{x:Bind ViewModel.Device.Status.BatteryStatus, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}">
                                <FontIcon Margin="0,0,4,0" Glyph="{x:Bind ViewModel.Device.Status, Converter={StaticResource BatteryStatusToIconConverter}, Mode=OneWay}" Foreground="{x:Bind ViewModel.Device.Status, Converter={StaticResource BatteryStatusToColorConverter}, Mode=OneWay}" />
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind ViewModel.Device.Status.BatteryStatus, Mode=OneWay}" />
                                <TextBlock FontWeight="SemiBold" Text="%" />
                            </StackPanel>
                        </StackPanel>

                        <!--  Connection Status Card with ADB Info  -->
                        <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                            <Button
                                HorizontalAlignment="Left"
                                Command="{x:Bind ViewModel.ToggleConnectionCommand}"
                                Content="{x:Bind ViewModel.Device.ConnectionStatus, Converter={StaticResource BoolToConnectionButtonTextConverter}, Mode=OneWay}"
                                Visibility="{x:Bind ViewModel.Device.Name, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
                            
                            <!--  ADB Connection Status Labels  -->
                            <ItemsRepeater ItemsSource="{x:Bind ViewModel.AdbConnectionTypes, Mode=OneWay}" Visibility="{x:Bind ViewModel.AdbConnectionTypes.Count, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}">
                                <ItemsRepeater.Layout>
                                    <StackLayout Orientation="Horizontal" Spacing="5" />
                                </ItemsRepeater.Layout>
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="x:String">
                                        <Border
                                            Padding="5,4"
                                            Background="{ThemeResource SystemAccentColor}"
                                            BorderBrush="{ThemeResource SystemAccentColor}"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Spacing="0">
                                                <TextBlock
                                                    FontSize="12"
                                                    FontWeight="SemiBold"
                                                    Foreground="White"
                                                    HorizontalTextAlignment="Center"
                                                    Text="{x:Bind Mode=OneTime}" />
                                                <TextBlock
                                                    FontSize="10"
                                                    FontWeight="Normal"
                                                    Foreground="White"
                                                    HorizontalTextAlignment="Center"
                                                    Text="adb" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <!--  No Device Connected View  -->
                <Grid
                    Grid.Row="0"
                    Grid.RowSpan="3"
                    Margin="18,20,10,10"
                    Visibility="{x:Bind ViewModel.Device.Name, Converter={StaticResource InverseEmptyObjectToVisibilityConverter}, Mode=OneWay}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!--  Device List Section  -->
                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock
                            Grid.Row="0"
                            Margin="0,0,0,12"
                            FontSize="18"
                            FontWeight="SemiBold"
                            Text="{helpers:ResourceString Name=AvailableDevices/Title}" />

                        <!--  Available Devices  -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsRepeater ItemsSource="{x:Bind DevicesViewModel.DiscoveredDevices, Mode=OneWay}">
                                <ItemsRepeater.Layout>
                                    <StackLayout Orientation="Vertical" Spacing="8" />
                                </ItemsRepeater.Layout>
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="models:DiscoveredDevice">
                                        <Button
                                            Margin="0,0,0,4"
                                            Padding="12"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Left">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <FontIcon
                                                    Grid.Column="0"
                                                    Margin="0,0,12,0"
                                                    VerticalAlignment="Center"
                                                    Glyph="&#xE8EA;" />
                                                <TextBlock
                                                    Grid.Column="1"
                                                    VerticalAlignment="Center"
                                                    FontWeight="SemiBold"
                                                    Text="{x:Bind DeviceName}" />
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </ScrollViewer>
                    </Grid>
                </Grid>

                <!--  Separator  -->
                <Rectangle
                    Grid.Row="1"
                    Height="1"
                    Margin="18,0,24,0"
                    Fill="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                    Visibility="{x:Bind ViewModel.Device, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
                <!--  Notifications  -->
                <userControls:NotificationsListControl
                    Grid.Row="2"
                    ViewModel="{x:Bind ViewModel}"
                    Visibility="{x:Bind ViewModel.Device, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch" />
            </Grid>
            <Grid Grid.Column="1">
                <NavigationView
                    Grid.Row="0"
                    Grid.RowSpan="2"
                    BorderBrush="Transparent"
                    IsBackButtonVisible="Collapsed"
                    IsSettingsVisible="True"
                    PaneDisplayMode="Top"
                    SelectionChanged="NavigationView_SelectionChanged">
                    <NavigationView.Resources>
                        <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                    </NavigationView.Resources>

                    <NavigationView.MenuItems>
                        <NavigationViewItem Content="应用" Tag="Apps" IsSelected="True">
                            <NavigationViewItem.Icon>
                                <SymbolIcon Symbol="AllApps" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                        <NavigationViewItem Content="本机通知历史" Tag="LocalNotificationHistory">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE8BD;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                        <NavigationViewItem Content="显示器亮度" Tag="MonitorBrightness">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE706;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                        <NavigationViewItem Content="DeepSeek余额" Tag="DeepSeekBalance">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE8BC;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                        <NavigationViewItem Content="覆盖层" Tag="DanmakuOverlay">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE7F0;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
<NavigationViewItem Content="动态光效" Tag="DynamicLighting">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE739;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                        <NavigationViewItem Content="音频中继" Tag="AudioRelay">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE995;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                    </NavigationView.MenuItems>

                    <Frame x:Name="ContentFrame" />
                </NavigationView>
            </Grid>
        </Grid>
        <win:Grid>
            <userControls:TrayIconControl />
        </win:Grid>
    </Grid>
</Page>