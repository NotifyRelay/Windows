using Microsoft.UI.Xaml;

namespace NotifyRelay.Extensions;

public static class WindowExtensions
{
    // Minimal stubs to satisfy build when WinUIEx or other libraries are not available.
    public static void SetWindowIcon(this Window window)
    {
        // Intentionally left blank - extension provided for compatibility
    }

    public static void UseStudio(this Window window)
    {
        // Intentionally left blank - extension provided for compatibility
    }
}
